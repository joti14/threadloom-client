// Demo account used by the "Demo Login" button on the login page and listed in
// the project's submission credentials. This is the seeded admin account, which
// owns the catalog and can manage items.
export const DEMO_CREDENTIALS = {
  email: "admin@threadloom.dev",
  password: "ThreadloomAdmin123!",
} as const;
